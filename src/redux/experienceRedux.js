//selectors
export const getExperience = ({ experience }) => experience;

// reducer
export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
